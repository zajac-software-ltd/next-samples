# Database - PostgreSQL
DATABASE_URL="postgresql://username:password@localhost:5432/database_name"

# NextAuth.js Configuration
NEXTAUTH_URL="http://localhost:3000"
NEXTAUTH_SECRET="your-secret-key-here-replace-in-production" # openssl rand -base64 64 or use a secure UUID generator

# Password Security
PASSWORD_PEPPER="your-password-pepper-uuid-here" # node -e "console.log(require('crypto').randomUUID())" or similar secure uuid generator

# Service-to-Service Authentication
SERVICE_JWT_SECRET="your-service-jwt-secret-here" # openssl rand -base64 64 or use a secure random string
MAIN_APP_SERVICE_SECRET="your-main-app-service-secret-here" # Plain text secret for enhanced auth
MAIN_APP_CLIENT_SECRET="your-main-app-client-secret-here" # Shared secret for main app token requests

